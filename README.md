# HitMode

# hitmole_boards 설명
# hitmole_board는 게임 시작을 알리는 함수로, HIT MOLE 글자판을 한 번 깜빡이도록 한다.
# os.system("clear")는 앞에서 짠 코드를 지워 빈 코드 판으로 만들어준다.
# time.sleep(초)는 정해준 일정 시간 동안 유지시키는 역할이다.
# tutorial_board는 튜토리얼 시작을 알리는 함수로, 위와 동일하게 글자판을 한 번 깜빡이도록 한다.
# start_board는 게임 시작 전 3,2,1로 카운트하게 만드는 함수로, 
# 숫자가 세 번 나타나고 사라져야 하기에 for문을 사용해서 코드 3번 반복하게 하고, 
# 한 숫자는 5행 5열으로 구성되어 있다.
# print_board(pop)은 랜덤으로 튀어나온 두더지를 출력하는 코드이고, 랜덤으로 숫자를 뽑아둔
# pop_up을 받아 해당 인덱스만 두더지를 출력하고, 나머지는 구멍을 출력하도록 한다.
# 첫번째 반복문은 3행을 위한 반복이고, 두번째 반복문은 한 행을 위한 반복이며 인덱스는 9개의 HOLE
# 중 한 곳을 뽑을 때 사용한다. 만약 맞췄을 때는 두더지가 맞은 모양인 HIT을, 아니면 HOLE이 나오게
# score_board(points)는 최종 점수를 출력하는 함수로, 글자판과 함께 점수 속성을 화면에 출력한다.
# point를 문자로 바꿔서 ROUND를 올릴지, 올리지 않고 끝낼지 결정한다.
# 숫자 완성을 위한 다섯줄을 위해 5번 반복문을 돌리고, 스코어 한 줄을 뽑고 난 후 옆에 숫자 한 줄 뽑는
# round_board는 게임의 라운드를 출력하며, 사용자가 일정 점수를 넘겨 다음 라운드로 진입할 경우
# 라운드를 출력한다. 
# over_board는 게임이 끝났다는 GAME OVER 글자판을 세 번 깜빡이도록 한다. 숫자가 필요없기에 
# _처리 했고, 세 번 깜빡여야 하므로 3번 반복문을 돌렸다. 

# hitmole2 설명
# LengthError는 닉네임을 6자 이내로 작성하라는 에러문
# AlphabetError는 영어를 소문자로 작성하라는 에러문
# __init__은 점수 카운팅을 위한 속성인 points와 입력키 판별을 위한 click, 게임 라운드 진행을 저장하는 cnt_round가 있다.
# pop_up은 랜덤으로 두더지가 나올 구멍을 뽑는 함수로, 뽑힌 수만 1 처리하고 나머지는 0으로 처리하# 는 행렬을 사용했다.
# wait_keyboard는 사용자로부터 키보드를 입력 받는 역할로, key.char 는 입력된 키보드값이 문자열이 아니면 오류가 남.
# 튜토리얼은 조작키를 알려주고 연습하는 메소드로 랜덤으로 나타난 두더지가 사용자가 올바른 키를 입력할 때까지 사라지지 않고 기다려주며, 멈추고 게임을 시작하고 싶으면 스페이스바를 누르도록 안내.
# self.click = "wrong key"인 경우가 있기 때문에 리스트 인덱싱 오류를 피해야 함.
# play_round는 한 라운드 안에서의 플레이로, 사용자가 두더지가 사라지기 전에 해당 키를 누르면
# 두더지가 찌그러지는 효과와 함께 사용자 점수를 +10, 지정된 9개의 키 외의 키를 누르면 기회를 더 준다.
# 랜덤으로 9개 중 하나를 받아 mole로 하고, ROUND에 따라 두더지가 머무는 시간이 다르기에 
# time_by_level = 1.0 - (0.2 * self.cnt_round)
# 만약 사용자가 맞는 키를 누르면 self.points += 10
# play는 전체 게임의 진행을 위함이고, tutorial-> start_board-> round 1-> 점수..->
# 이렇게 점수를 판별하여 라운드를 높일 것인가 아님 종료할 것인가를 판별하고
# 이 과정을 반복한 후 끝나면 랭킹을 출력하는 함수이다.
